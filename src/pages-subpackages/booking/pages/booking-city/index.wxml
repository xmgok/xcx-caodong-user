<view class="page">
  <view class='wrapper'>
    <list-html
      data="{{city}}"
      binddetail="bindtap"
      isShowMyCity="{{true}}"
      my-city="{{myCity}}"
      config="{{config}}"
    ></list-html>
  </view>
</view>
